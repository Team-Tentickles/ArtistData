# ArtistData
Initial data for the artists that will be loaded by the form

This repository is formatted as an NPM package, so you can install it as a dependency using:
```
npm install Team-Tentickles/ArtistData
```
